# Metastable Failures in Distributed Systems (HotOS 2021)

- 作者: Nathan Bronson (Rockset, 前 Facebook), Abutalib Aghayev, Aleksey Charapko, Timothy Zhu (Penn State / UNH)
- 出处: HotOS '21, https://sigops.org/s/conferences/hotos/2021/papers/hotos21-s11-bronson.pdf
- 性质: vision paper —— 首次把"亚稳态故障"定义为一类系统性故障模式,提出统一分析框架,并指出系统性防御是开放问题。

## 核心概念:亚稳态故障 (Metastable Failure)

**定义**:在开放系统(负载来源不可控)中,某个**触发器 (trigger)** 使系统进入一个坏状态;即使触发器消失,系统仍因**维持效应 (sustaining effect)**(通常是工作放大 work amplification 或整体效率下降)困在该状态,goodput(有效吞吐)低到不可用。退出该状态需要强力纠正措施(重启、大幅降载等)。

- 触发器消失后自愈的故障(DoS、limplock、livelock)**不是**亚稳态故障。
- 事故发生后人们常把锅归给 trigger,但**真正的根因是 sustaining effect**(维持反馈环)。

**三阶段生命周期**(论文 Figure 1/2):

1. **Stable(稳定态)**:负载低于某个隐含阈值,触发器来了也能自愈。
2. **Vulnerable(脆弱态)**:负载超过阈值后系统表面健康,但一个触发器就可能把它推进亚稳态。**阈值是隐含且不可见的**;很多生产系统为了效率长期运行在脆弱态,可运行数月数年不出事。
3. **Metastable(亚稳态)**:反馈环自我维持故障,甚至跨 shard/集群/数据中心传染;需要足够强的纠正动作才能恢复。

关键概念:

- **Hidden capacity(隐含容量)** = 稳定态与脆弱态的边界,即系统能自愈的负载上限。它由系统在故障状态下的行为决定,正常运行时难以测量。
- **Advertised capacity(标称容量)** = 系统能服务的上限(此时已在脆弱态)。例:有 90% 命中率 cache 的 web 应用标称 3000 QPS,但 cache 全失后 DB 只能扛 300 QPS —— 隐含容量只有 300。
- **Trigger intensity(触发强度)**:越接近标称容量,越小的触发器就能引发故障。
- **Characteristic metric(特征指标)**:被触发器打 spike、直到亚稳态解除才恢复的指标(如排队延迟、超时率、cache 命中率、锁竞争等)。是观测/告警/实验测隐含容量的抓手;queueing delay 比 QPS 这类指标对负载变化更鲁棒。

## 四个生产案例(§2)

1. **请求重试 (Request Retries)**:DB 在 <300 QPS 时正常,超载后延迟恶化 10 倍;应用超时 1s 重试。一次 10s 网络中断恢复后,积压请求+重试形成持续 560 QPS 冲击,DB 永远恢复不了,goodput=0。负载 >150 QPS(DB 容量/重试放大倍数)即进入脆弱态。**Failover(故障转移)是近亲**:不放大单个请求,但会把故障级联传染到未受影响的副本。
2. **Look-aside Cache**:cache 丢失 → DB 变慢 → 应用超时,而应用超时又导致它不去回填 cache → cache 永远冷 → 持续 10x 查询放大。**Read-through cache 没有这个死结**(cache 自己负责回填,可以容忍超时)。
3. **慢速错误处理 (Slow Error Handling)**:成功路径高度优化,错误路径为调试方便做重活(抓栈、DNS 反查、写磁盘日志、发集中日志)。触发器耗尽错误处理所用的资源(CPU/线程/磁盘/带宽)后,错误处理本身加剧短缺,形成自持错误状态。
4. **链路不均衡 (Link Imbalance)**:Facebook 真实案例,困扰两年多、多次误判根因。聚合链路按四元组 hash 分配 TCP 连接;cache miss 突发时,拥塞链路上的连接**最后完成**,而连接池是 MRU 策略 —— 最后归还的连接排在最前,后续流量全被导向已拥塞的链路,反馈环闭合。修复只需一行(改连接池策略),但定位需要应用层和网络层工程师交换实现细节:任何一方的简化模型都不足以理解问题。

共同特征:**维持效应几乎总与某种资源耗尽相关,而反馈环往往是由提升稳态效率/可靠性的 feature(重试、cache、failover、优化)亲手创造的**。

## 已有的应对手段(§3,ad-hoc)

- **根因观**:修 sustaining effect 而非 trigger(trigger 有无数种)。
- **过载时切换策略**:过载期间禁用/预算化重试(retry budget)、禁用 failover、LIFO 调度让部分请求赶上 deadline、减小内部队列、优先级、按比例丢弃负载、熔断。难点:(a) 各客户端分散决策 vs 需要全局信息;(b) 区分持续过载与瞬时尖峰 —— 论文推荐像 CoDel 一样测量滑动窗口内的**最小排队延迟**(窗口内队列曾排空 ⇒ 只是尖峰)。
- **优先级**:重试流量降级可打断重试环;但优先级系统只能覆盖部分资源,且可能纵容高放大设计(某地理分布式系统靠重试+failover 叠加出 100x 最坏放大,仍被一个小后端拖垮)。**架构决定可实施的优先级**:软件结构本身就编码了隐式优先级(如先尽量反序列化再处理 = 反序列化优先,与过载期保 goodput 的要求相反)。
- **压测**:小规模压测信心有限(反馈环强度随规模的常数因子变化);更有效的是像 Kraken 那样在生产上精细引流压测,且 tooling 顺带提供出故障时快速排空集群的能力。
- **组织激励**:只优化 common case 的改进会诱使系统运行在离脆弱阈值更近的位置("虚假容量收益");奖励"减少冷 cache miss"才是真容量。
- **快速错误路径**:错误处理也要高性能 —— 用有界无锁队列把错误发给专用日志线程,溢出只计数;栈信息等昂贵操作采样/限流。
- **Outlier hygiene**:亚稳态故障的根因往往先以延迟毛刺/错误簇的形式小规模预演,值得追查。
- **Autoscaling**:扩容缓冲可降低对多数触发器的脆弱性,但有状态系统的重配置(数据搬迁)短期内反而降容量,可能来不及打断反馈环。

## 开放研究方向(§4)

1. 如何从软件框架层面让有害反馈环不可能发生、或至少可被发现(系统性理解并给 work amplification 加上界;识别并削弱最强的反馈环即可,不必消灭全部)。
2. 系统性识别脆弱点:找到系统的 characteristic metrics。
3. 估计新型亚稳态故障发生概率:特征指标 + 安全区间告警;实验法测 hidden capacity(加压 → 施加 trigger → 观察特征指标是否自愈回落)。
4. 弹性云基础设施能否用于恢复(零影响弹性、资源耗尽期的重配置)。
5. 非全尺寸复现/建模:抽象和聚合统计会掩盖病因;思路是控制测试环境使其**特征指标**与全尺寸匹配;负载发生器要避免 coordinated omission。
6. 量化实现/配置变更对反馈环强度的影响。

## 启示

- "能解释上一次,预测不了下一次" —— 论文目标是改变这一点,但到论文发表时仍是开放问题。
- 效率优化与可靠性 feature 本身是故障温床:长期高效运行 = 长期运行在脆弱态。容量规划应以 hidden capacity 而非 advertised capacity 为安全线。
- 对推理服务系统(如 `~/source_code/vllm/`、`sglang/`、`mooncake/` 这类有重试、KV cache、PD 分离、弹性扩缩容的系统)同样适用:KV cache 丢失/命中率骤降、prefill/decode 队列过载时的重试与 failover、自动扩缩容的冷启动延迟,都是典型的"feature 变反馈环"场景;排队延迟类指标(如 TTFT 排队时间)是比 QPS 更鲁棒的特征指标。
