# Overload Control for μs-Scale RPCs with Breakwater (OSDI 2020)

- 作者: Inho Cho, Ahmed Saeed, Joshua Fried, Seo Jin Park, Mohammad Alizadeh, Adam Belay (MIT CSAIL)
- 出处: OSDI '20, https://saeed.github.io/files/breakwater_osdi20.pdf,开源: https://inhocho89.github.io/breakwater/
- 性质: 系统论文 —— 针对微秒级 RPC 的**服务器驱动、基于信用 (credit) 的准入控制**过载控制方案;过载控制/负载丢弃 (load shedding) 方向的代表工作。

## 问题设定:为什么 μs 级 RPC 的过载控制是难题 (§1-2)

**背景**:现代数据中心应用由数百个微服务组成、扇出 (fanout) 大,对尾延迟敏感。内存驻留型服务(KV store、内存数据库)的 RPC 服务时间只有微秒级,单机要处理来自上千客户端的百万级 QPS。过载会导致 **receive livelock**:服务器忙于处理新到包的中断而饿死队列中已排队请求,goodput 趋零;还有 **RPC incast**(大量客户端同时发请求造成服务器队列暴涨)。

**核心矛盾**:对 μs 级 RPC,"传递拥塞信息 + 丢弃一个请求"的开销 ≈ "处理一个请求"的开销。两类已有方案都失效:

1. **AQM(服务器/代理侧丢弃,如 CoDel)**:被丢请求仍消耗包解析、生成失败响应的 CPU;客户端越多,可合并的失败响应越少,开销越大。实验(平均服务时间 10 μs、SLO 200 μs)显示负载超过容量后 CoDel 的 goodput 持续下滑,且客户端越多越糟。
2. **客户端限速(如 SEDA、ORCA 的窗口式 AIMD)**:客户端对服务器拥塞状态的感知至少滞后一个 RTT,而 RTT 与 μs 级服务时间相当;客户端越多,单个客户端消息频率越低、视图越陈旧,容易集体超调引发 incast。窗口式方案还有"每客户端至少 1 个在途请求"的下限,万级客户端时总并发依然压垮服务器。

**μs 级场景特有的四个挑战**(§2.3):服务时间短(控制环要以 μs 粒度反应)、服务时间长尾(单个长请求即可造成显著排队,队列长度信号失真)、需求波动(客户端需求零散,精确授信会导致利用率不足)、客户端数量大(加剧以上全部)。

**与网络拥塞控制的区别**:目标都是"短队列 + 高利用",但 (a) RPC 服务时间方差大而包处理时间恒定;(b) RPC 客户端可能超时放弃或转投备份服务器,需求波动远大于网络流。所以不能照搬拥塞控制。

## 理想过载控制的五条性质 (§2.1)

1. 吞吐无损:过载时仍以满容量处理,控制开销极小;2. 低排队延迟;3. 可扩展到大客户端数、抗 incast;4. 低丢弃率(丢弃浪费服务器资源且伤害尾延迟,重试代价高);5. 快速反馈:服务器若预判请求会违反 SLO,应尽早拒绝,让客户端立即采取备选动作(放弃、换副本、降级)而非等超时。

## Breakwater 设计 (§3)

灵感来自数据中心网络中的接收端驱动拥塞控制(Homa、NDP、ExpressPass):把"谁可以发"的决定权从发送方移到接收方(服务器)。

### 过载信号:排队延迟 (§3.1)

- 不用 CPU 利用率(无法区分"100% 利用且无排队"与 livelock,也覆盖不了磁盘等资源争用);不用队列长度(服务时间长尾时与延迟对应关系差);用**排队延迟 (queuing delay)**——对服务时间方差鲁棒,且天然可映射到 SLO。
- 实现上跟踪两级排队(包排队 + 线程排队)之和:每个队列维护队头元素的最早时间戳,延迟 = 当前时间 − 该时间戳。
- 线程模型选 dispatcher 模型(分发线程 + 每请求一个轻量线程)而非 in-line 模型,避免单个长请求造成的队头阻塞污染延迟测量。依赖 Shenango 的低延迟运行时(每 5 μs 重分配核、用户态轻量线程)。

### 服务器驱动的信用准入控制 (§3.2.1)

- 客户端只有拿到服务器的 credit 才能发一个请求。服务器维护全局信用池 `Ctotal`,表示"在不违反 SLO 的前提下能承受的负载"。
- 每个 RTT 按 AIMD 调整 `Ctotal`:测得排队延迟 `dm` < 目标 `dt` 则加性增 `Ctotal ← Ctotal + A`;否则按过载程度乘性减 `Ctotal ← Ctotal · max(1 − β·(dm−dt)/dt, 0.5)`。
- credit 尽量**捎带 (piggyback) 在响应里**下发;要缩池时不再发新 credit,或向持有未用 credit 的客户端发**负 credit 回收**(服务器记账:已发 credit 数 − 已收请求数 = 各客户端未用 credit 数)。

### 需求推测 + 超量授信 (§3.2.2)

- 精确做法(客户端每次需求变化都上报)的消息开销随客户端数压垮服务器。Breakwater 改为**零额外协调消息**:需求信息只在有请求可发时捎带上报,服务器基于(可能陈旧的)最近需求**推测性授信**。
- 推测会发给"此刻没需求"的客户端 → credit 闲置 → 利用率下降。对策是**超量授信 (overcommitment)**:加性增量 `A = max(α·nc, 1)` 与客户端数成正比;每客户端允许持有 最近需求 + `Coc` 个 credit,`Coc = max((Ctotal − Cissued)/nc, 1)`。
- 代价:超发可能引发 incast → 由 AQM 兜底。

### 延迟驱动的 AQM (§3.2.3)

排队延迟超过 SLO 推导的阈值(实验取 `2·dt`)时,在**解析完请求后尽早丢弃**并立即回拒绝消息。由于信用机制本身不产生突发,实际丢弃很罕见。

### 客户端行为 (§3.3)

客户端本地排队等 credit,并按 SLO 设置**请求过期时间**——在客户端侧就让注定违反 SLO 的请求过期,兼顾端到端延迟与吞吐。新客户端注册时捎带首个请求和队列长度;退出时注销并归还未用 credit。

### 实现要点 (§4)

基于 Shenango 的 RPC 库(TCP 之上);每应用一个 RPC 层(全局 credit 池);listener/receiver/sender 线程分工,sender 合并响应;"惰性信用分配"把 max-min 公平分配的 O(N) 扫描摊到各 sender 线程发响应时按需计算(公式 5/6),避免准入控制器逐客户端扫描。

## 评测要点 (§5)

测试床:CloudLab,RTT 10 μs,SLO 设为 10×(平均服务时间 + RTT)(1/10/100 μs 服务时间对应 110 μs/200 μs/1.1 ms SLO)。对比 DAGOR(微信的优先级过载控制)和 SEDA(客户端限速),参数均调到最优。默认参数:`α=0.1%, β=2%, dt=40% SLO`,AQM 阈值 `2·dt`。

- **合成负载**(1000 客户端、指数服务时间、平均 10 μs,服务器容量约 850k req/s):需求 2× 容量时,Breakwater 比 DAGOR 高 6.6% goodput、99 分位延迟低 1.9×;容量点附近控制开销约 5%。
- **负载突变**(需求从 0.5× 跳到 1.4× 容量):Breakwater **<20 ms 收敛**且不发生拥塞崩溃;DAGOR 拥塞崩溃、500 ms 恢复,崩溃期 99 分位延迟飙到 300 ms、拒绝消息延迟 220 ms(客户端只能靠昂贵超时兜底);SEDA 1.58 s 才恢复。负载回落时 Breakwater goodput 无感,DAGOR/SEDA 分别暂时掉到收敛值的 77.5%/82.6%。
- **可扩展性**:100→10,000 客户端,各方案 goodput 都随客户端数下降,但 Breakwater 降得最少;且 Breakwater/DAGOR 不需要按客户端数重调参,SEDA 需要(其客户端侧算法还得知道全局客户端数,实践中不可行,也有隐私问题)。
- **memcached 真实负载**(USR 负载,10,000 客户端,SLO 50 μs):需求 2× 容量时比 DAGOR 高 14.3% goodput、99 分位低 2.9×;比 SEDA 高 5%、低 1.8×。
- **消融**:信用准入管住尾延迟但消息开销伤吞吐 → 需求推测+捎带省掉消息开销但超发引发 incast → AQM 解决 incast。三者缺一不可。
- **参数鲁棒性**:对 dt/α/β 做 ±10 μs、0.5×/2× 扰动共 27 组配置,Breakwater 性能几乎不变;DAGOR 最坏尾延迟达 4× SLO,SEDA 参数保守时吞吐严重受损。可用更保守参数换低丢弃率(如 β=8% 时 0.7% 丢弃率、牺牲 2.2% goodput)。
- **局限/讨论**:不考虑自动扩缩容(分钟级,对 μs 级失衡太慢);信用不足时偏好正在服务的客户端(为捎带省消息),公平性需更长时间尺度上的轮转;多层微服务场景下中间层过载会让上游已做工作作废,跨层传递过载信号留作未来工作。

## 与 HotOS'21 亚稳态故障框架的关系

Breakwater 是 metastable failures 框架下"过载时切换策略 / 负载丢弃"这一类防御手段的**机制化、正面样板**:

- **它预防的正是亚稳态**:receive livelock 与 congestion collapse(队列暴涨 → 全部请求违反 SLO → goodput 归零)是典型的"坏状态";DAGOR/SEDA 实验中负载回落后 goodput 仍暂时下跌的现象,就是短暂的自持效应。Breakwater 用准入控制把系统**永远挡在脆弱阈值之外**,而不是事后纠正。
- **信号选择互相印证**:metastable failures 论文指出 queueing delay 比 QPS 对负载变化更鲁棒、适合作特征指标;Breakwater 独立地得出了同一结论——CPU 利用率分不清"健康满载"和"livelock",队列长度扛不住服务时间长尾,只有排队延迟既鲁棒又可直接映射 SLO,于是用它做控制信号。
- **快速失败打断反馈环**:Breakwater 的"尽早拒绝 + 客户端侧请求过期"对应框架里的 retry budget / fail-fast——让客户端在超时前就拿到拒绝,避免超时重试放大负载(框架案例 1 的重试环);DAGOR 崩溃期 220 ms 的拒绝延迟正是反面教材。
- **反馈环来自好心设计**:超量授信(为利用率)会制造 incast,必须靠 AQM 兜底——与框架"提升稳态效率的 feature 亲手创造反馈环"的观察同构。
- 差异:Breakwater 做的是单机、单层、同构请求的准入控制,不处理 metastable failures 强调的多组件传染(work amplification 跨层传播、cache 类反馈环),论文自己也把跨层过载信号传播留为开放问题。

## 启示

- **排队延迟应作为推理服务的一等控制信号**。对 vLLM/SGLang 这类系统,TTFT 排队时间(prefill 队列等待)就是天然的 `dm`:它比 QPS、GPU 利用率更能区分"健康满载"与"过载崩溃",且可直接映射到 SLO。调度器可以像 Breakwater 一样用 AIMD 围绕目标排队延迟调整准入阈值,而不是靠人工调的 max-num-seqs / 队列上限。
- **准入决定权应放在最了解拥塞的一端**。PD 分离架构(vLLM + mooncake、MindIE-Motor)里,prefill/decode 节点各自掌握自己的队列深度与 KV cache 压力,由服务端(或 router 代理的服务端视图)发放"准入额度"比客户端/网关侧盲目限速更准确;Breakwater 证明服务端驱动方案在客户端数大、需求零散时优势明显,而且服务端天然知道全局客户端数,无需暴露。
- **超量授信 ⇔ continuous batching 的过度乐观**:为提高利用率而激进入坞(多排 prefill、超发额度)必然偶发 incast,需要配一个"尽早丢弃"的兜底机制(对推理服务:TTFT 预估超 SLO 即早拒,返回明确失败让客户端立即换副本/降级,而不是排队到超时)。早拒的成本远低于占用 prefill 算力后再放弃。
- **零协调开销的需求传递值得借鉴**:需求信息捎带在请求里、额度捎带在响应里,避免独立的控制面 RPC。对 mooncake 这类 KV cache 传输层,其 receiver-driven 传输本身就有 credit 语义,Breakwater 的经验(惰性分配、按需计算、负 credit 回收)可直接参考。
- **参数鲁棒性是生产可用的硬指标**:流量模式随时变,不可能持续重调参。Breakwater 对参数 ±10 μs/0.5×/2× 扰动不敏感这一点,比峰值性能数字更值得推理系统的过载控制模块学习。
- **分钟级自动扩缩容救不了秒/毫秒级过载**:扩缩容只能改变长期容量基线,短时过载必须靠准入控制与负载丢弃;两者是互补关系,不能互相替代。
