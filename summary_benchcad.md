# BenchCAD: A Comprehensive, Industry-Standard Benchmark for Programmatic CAD

- **arXiv**: https://arxiv.org/abs/2605.10865 (May 2026, NeurIPS 2026 Datasets & Benchmarks preprint)
- **作者**: Haozhe Zhang, Kaichen Liu, Miaomiao Chen 等（UVA / UCSD / Rice）
- **发布**: 数据 `BenchCAD/BenchCAD` (Hugging Face, CC-BY-4.0)；评测代码 `BenchCAD/BenchCAD-main` (GitHub, MIT)；项目页 https://benchcad.github.io/BenchCAD_webpage/

## 一句话总结

BenchCAD 是一个面向工业 CAD 代码生成的评测基准：17,900 个经过执行验证的 CadQuery 参数化程序，覆盖 106 个工业零件族（锥齿轮、压缩弹簧、麻花钻等），通过 4 类任务、4 层能力层级把"生成的形状看起来像"和"程序真的符合工程设计"区分开来。结论：当前前沿多模态大模型（MLLM）能恢复零件的大致外形，但在细节结构、CAD 操作选择和工业参数抽象上系统性失败——"过了眼睛，过不了卡尺"。

## 动机与核心观点

1. **单一几何指标（IoU / Chamfer）会高估能力**。两个程序可能外形包络相似，但可编辑性、操作选择、工程细节完全不同。DeepCAD 系方法（CAD-Recode/cadrille/CADEvolve）已把 sketch-and-extrude 语料上的 IoU 刷到 ~92%，但那是被单一指标掩盖的假象。
2. **已有语料的操作面太窄**：几乎所有已发布 CAD 语料都被 sketch+extrude 主导，螺旋扫掠（helical sweep）、放样（loft）、twist-extrude、参数化渐开线齿轮构造完全缺失。
3. **需要能力分解的评测**：CAD 失败是组合性的——模型可能认对零件族却选错操作、估对尺度却违反标准参数关系。BenchCAD 提出 4 层能力层级：
   - **L1** 整体视觉识别（从多视图渲染认出零件族）
   - **L2** CAD 操作理解（把几何映射到正确的操作及执行顺序）
   - **L3** 工业参数抽象（恢复工程师会写的参数结构与标准表约束）
   - **L4** 组合式空间-代码推理（规划空间一致的操作、维护参数依赖、产出可执行代码）

## 数据集设计（4 条原则）

- **P1 专家构造**：每个零件族由领域专家直接从工业标准出发，手工求解标准规定的几何方程，实现为带类型化参数 schema、采样器、验证器和确定性 builder 的 Python 模块。
- **P2 标准锚定**：49%（52/106）的零件族绑定真实 ISO/DIN/EN/ASME/IEC 规格表（如 ISO 22 V 带截面、DIN 338 钻头直径、ISO 23509 锥齿轮模数关系），共引用 47 个标准代号。
- **P3 族级分类法**：106 个命名族（coil_spring、helical_gear……），族下可再分子族（如公/母紧固件），使覆盖率可度量、可做逐族分析。
- **P4 操作广度**：覆盖 49 种 CadQuery 操作，包括 `makeHelix`、`twistExtrude`、`polarArray` 等在 DeepCAD/Fusion360 系语料中罕见或没有的操作。

**生成管线**：每族分 easy/medium/hard 三档难度（按参数复杂度，高档扩大参数范围并激活可选特征）；采样参数 → 生成 CadQuery 程序 → 沙箱执行（编译失败、超 30s、体积退化者隔离）→ 专家视觉验收。三个发布物：BenchCAD 主集 17,900 条；BenchCAD-QA 2,400 条图像/代码配对的数值问答；BenchCAD-Edit 748 条编辑对（尺寸型、增材、减材、多步）。

## 四类任务与指标

| 任务 | 输入→输出 | 主要指标 |
|---|---|---|
| **Vision2Code** (`img2cq`) | 4 个正交视图 → CadQuery 代码 | 体素 IoU、Chamfer/Hausdorff 距离、Feature-F1、关键操作召回 `ess`、执行率、综合 `total` |
| **Code Edit** (`edit_code`) | 原程序+自然语言指令 → 修改后的程序 | 归一化准确率：Acc = clip((IoU(gen,tgt) − IoU(orig,tgt)) / (1 − IoU(orig,tgt)), 0, 1)，即"走完原样到目标差距"的比例；编辑类型 T1–T5（字面替换、链式变换、相对计算、特征编辑、几何重建） |
| **Vision QA** (`qa_img`) | 渲染图 → 数值答案 | ±5% 容差（比率）/精确匹配（整数），按 L1–L4 分解 |
| **Code QA** (`qa_code`) | CadQuery 代码 → 同样的问题 | 同上，与 Vision QA 构成配对对照 |

配对设计是诊断核心：同一问题图像版远差于代码版 → 瓶颈在视觉识别；代码版也差 → 瓶颈在 L2 操作理解。每个任务还有"盲基线"（黑图、无代码、原样返回）来衡量超出数据集先验的真实增益。

## 主要实验结果（10+ 前沿模型 + CAD 专精模型）

1. **Vision2Code**：CAD 专精系（cadrille-RL、CADEvolve）在 IoU 上好但非 extrude 操作差；前沿 MLLM 上限中等（gemini-3.1-pro-thinking total 仅 0.318）；thinking 模式行为非单调。
2. **Code Edit 比 Code Gen 更难**：所有模型都是编辑落后于从零生成。简单 API 级编辑（T1）接近解决，组合型编辑（T5）仍很难。把文字指令换成目标渲染图，所有模型崩溃到接近零（渲染给形状不给数值）；给指令附上原图反而几乎无帮助甚至损害最强 thinking 模型——文字指令才是主力，视觉信号顶多是澄清。
3. **QA 模态差距**：Code QA 最好约 0.838 vs Vision QA 0.587——直接看代码远容易于从渲染图推理参数；但即使有代码，L4 空间/代码推理仍最弱。
4. **Family Cliff**：按难度分层，最强模型从 easy 到 hard 通过率塌掉 >45 分（claude-opus-4.7：easy 57.1% → hard 45.5%；qwen3-VL-32B：easy 35.3% → hard 0%）。hard 族的 26 个零件族需要螺旋扫掠、扭转拉伸、放样布尔等高级操作和齿轮模数-齿数-节距一致性等参数约束，没有模型能稳定满足。
5. **错误分析（三类核心缺陷）**：
   - **整体空间识别（L1）**：GPT-5.3 生成六角螺栓时丢了螺纹和顶面倒角；轴承保持架端盖被从 XY 平面而非 XZ 平面拉伸——形状对但参考系错（24 轴旋转不变 IoU 远高于单轴 IoU 佐证了这一点）。
   - **操作理解（L2，Op Vocabulary Gap）**：扭转支架被生成为两个互相垂直的支架，程序里根本没有 twist-extrusion。
   - **工业参数抽象（L3）**：DIN 2095 弹簧要求并紧磨平端（端部螺距局部减小），模型认出了弹簧也用了螺旋扫掠，却把端部变螺距塌缩成均匀螺旋——标准驱动的参数化被丢失。
   - 编辑任务另有 F01–F08 八类失败码（错参数值、错特征实例、错轴向、selector 选错面、near-no-op 原样重写、多实例更新不全等），按能力层归因。
6. **作为训练资源**：用 BenchCAD 训练 Qwen3-VL-2B（SFT + GRPO 式 RL，奖励 r = 0.2·ess + 0.8·IoU，解析失败 −1），ID 代码生成达 0.7631（全场最佳）；留出 10 个机械族做 OOD 仍有非平凡迁移，并扩大了操作词汇（revolve/sweep/loft/fillet 召回提升）；但 IID/OOD 差距依然巨大——新零件族的泛化仍是开放问题。

## 局限

- 零件来自专家编写的标准参数族，不是真实工程仓库提取，不覆盖私有工业设计的全部多样性（公差标注、设计意图、装配信息）。
- "标准锚定" ≠ "标准合规"：只在标准参数范围内采样并满足参数间关系，不验证全套制造公差。
- 非目标保留指标用体素 IoU，亚体素的小参数偏移可能漏检。

## 可借鉴之处

这篇论文与 nanochat 这类 LLM 训练项目不直接相关，但其评测方法论有通用价值：

1. **能力分解 > 单一端到端分数**：把"答案对不对"拆成感知/操作映射/参数抽象/组合推理四层，用配对对照（同一问题的图像版 vs 代码版）定位瓶颈。做任何"生成可执行代码"类评测（如 Text2SQL、代码修复）都可以照搬这个思路。
2. **盲基线**：去掉信息输入、只保留输出格式的基线，用于暴露数据集先验和指标地板——便宜但很少有人做。
3. **编辑任务的归一化指标**：`Acc = (IoU_gen − IoU_orig)/(1 − IoU_orig)` 这种"headroom 归一化"技巧可推广到任何"从 A 改到 B"的任务，消除样本难度差异。
4. **执行验证 + 语义失败码**：不仅检查代码能跑，还用手工标注的 F01–F08 语义失败码把失败归因到能力层，比"pass/fail"信息量大得多。
5. **训练侧启示**：RL 奖励混合"关键操作召回 + 几何 IoU"而非纯结果匹配，鼓励模型使用正确的高级操作而非走捷径；op-coverage 作为训练数据质量的度量维度值得借鉴。
