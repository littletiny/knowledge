# Analyzing Metastable Failures (HotOS 2025)

- 作者: Rebecca Isaacs (AWS), Peter Alvaro (UC Santa Cruz & AWS), Rupak Majumdar (MPI-SWS & AWS), Kiran-Kumar Muniswamy-Reddy (AWS), Mahmoud Salamati (MPI-SWS), Sadegh Soudjani (MPI-SWS)
- 出处: HotOS '25 (Banff, 2025-05), https://sigops.org/s/conferences/hotos/2025/papers/hotos25-106.pdf , DOI 10.1145/3713082.3730380
- 性质: 系统方法论论文 —— HotOS'21 "Metastable Failures" 提出开放问题(如何在系统出事**之前**识别亚稳态脆弱区)之后,这篇由 AWS 与学术界合作给出第一套实用答案:一条"抽象到具体"多级分析工具链,用便宜模型的预测驱动昂贵实验的搜索,再用实验 ground truth 反向校准模型。

## 问题定位:从"理解"到"预测"

- 过去十年的进展路径:民间轶事 → 定义与刻画(HotOS'21 [6]) → 分类、建模与真实事故复现(OSDI'22 "Metastable failures in the wild" [12]、SRDS'24 MSF-model [10])。但**识别与复现的进展尚未转化为分析与预测能力**:大规模级联故障依然频发(文中点名 OpenAI 与 AWS Kinesis 的公开事故)。
- 直接靠测试找亚稳态脆弱点不可行:参数空间是**静态配置 × 动态行为**(随机到达、超时、重试)的笛卡尔积,全空间压测成本太高;而 fuzzing/模型检查类工作基本不覆盖这类故障。必须先缩小搜索空间。
- 目标系统形态:**请求-响应系统 = 请求队列 + 线程池**(控制面服务的典型架构,也是重大亚稳态事故的重灾区)。

## 核心方法:多级抽象工具链(Figure 1, Table 1)

同一套系统描述,在逐级升高的保真度下被分析。共享语言是一个嵌入 Python 的 **DSL**(抽象:thread pool、queue、request、retry policy;业务逻辑被替换为**服务时间分布**——只建模线程被占用多久)。作者研究了多个生产请求-响应系统,发现"数量少得惊人"的核心抽象就足以刻画其高层动态。

| 工具 | 表达能力 | 成本 |
|---|---|---|
| 可视化 | 队列/重试状态空间动态 | 极低(ms 级) |
| CTMC | 数学宏观描述,预测全局平均行为 | 低(秒~分钟) |
| 仿真 DES | 核心抽象的真实实现(线程池调度等) | 中(分钟) |
| 仿真器 Emulator | 生产 RPC 框架 + 真实云部署,无业务逻辑 | 高(小时) |
| 真实系统 | 完全保真 | 极高(小时+) |

**双向闭环**:抽象工具的预测指导具体工具的实验点选择(向左→右);具体工具的观测反馈校准抽象模型(向右→左)。

### Running example(Figure 2)

服务器队列上限 150、单线程、容量 10 RPS;客户端正常负载 5 RPS、超时 5s、重试 5 次。问题:什么参数下系统会"翻倒 (tip)"?如何调参防御?

### CTMC(§3):全局视角 + 亚稳态的数学定义

- 状态 = (队列长度, "in orbit" 重试中请求数)。**orbit** 是 retrial queueing 理论的概念,直观刻画系统内的**潜在工作放大**。
- 由 CTMC 自动生成二维向量场可视化(Figure 3):每个 (x, y) 格子的箭头是最可能的下一步转移方向,颜色是概率强度。可清楚看到**拐点 (inflection point)**:5 RPS 负载下约在队列长度 40 + 在轨重试 30 处,越过该点的轨迹大概率进入自持反馈环;左下区域则收敛回空队列(安全)。生成只需毫秒级 → 运维可**交互式扫配置空间**,做离线仿真也负担不起的多维参数扫描(成千上万次 CTMC 分析 → 几十个 DES 运行确认)。
- 定量问题:稳态平均队列长度(平稳分布)、从满队列出发的期望恢复时间,基于线性代数,分钟级完成。
- **亚稳态的精确定义**(本文贡献之一):CTMC 存在两个以上互不相交的状态子集,彼此可达,但从一个子集到达另一子集的**最小期望时间远大于**从任意状态到达某个子集的最大期望时间——系统会在子集内"卡住"远超自然时间尺度。这把控制论/动力系统中的 metastability 概念翻译到了软件系统分析。
- 局限:到达/服务率都按指数分布建模;精确的重试计数与个体超时被合并成期望相同的概率分布——**工作放大越大,分布偏差越大**,定量预测会明显偏离仿真/实测。
- 实现开源:http://gihub.com/mpi-sws-rse/metafor (原文如此,疑似 github 拼写笔误)。

### 离散事件仿真 DES(§4):甜点位

- DES 相当于按 CTMC 状态空间的一条随机轨迹"重放"系统,但核心抽象(线程池调度、超时、重试)是操作性实现,而非分布近似。介于数学模型与真实基础设施之间的**sweet spot**:既验证预测,又是**白盒**——可抓任意指标、做单请求粒度的分布式追踪,作者观察到这常是运维理解亚稳态故障的 "aha" 时刻。
- 实验:基线 5 RPS 5 分钟 → 触发器 15 RPS 持续 10 分钟 → 回落 5 RPS 30 分钟。CTMC 预测队列上限 150 时**不可恢复**、35 时可恢复;DES 均确认(图 4a/4b,99 分位延迟时间序列)。卡死点(队列150 那个 run)在 Figure 3 状态空间上的落点用红圈标出。

### 仿真器 Emulation(§5):补上模型外因素

- 用内部广泛部署的 RPC 框架部署"空业务"服务(收到请求只按可配置分布 sleep,支持合成分布或生产指标分布),配高吞吐负载发生器,跑在与真实系统相同的云基础设施上——把模型忽略的因素(处理器资源竞争、线程调度、负载均衡器行为等)纳入检验。
- 结果与 DES 大致吻合(图 4c),但有一个正在调试的差异:**触发期后重试流量占比 emulator 比 DES 高 20%**——正说明 emulator 这一级不可省略。
- 经验对比:OSDI'22 复现真实亚稳态故障据说是"heroic work";本文借助 CTMC+DES 指引,写 Python 脚本扫参数就轻松找到引发同等级故障的配置。

### 真实系统压测(§6)

- 最贵但最有说服力:对运维而言,**没有被真实系统确认之前,建模/仿真/模拟结果都没有实际价值**。
- 但运维反馈:哪怕只给出**一个**能按需触发亚稳态故障的配置+负载设置就很有价值。当前工具输出即此;未来设想生成自动化压测脚本、接入持续测试流水线。

## 讨论(§7):普适性与校准

- **适用性**:HotOS'21 的其他案例(慢速错误处理、链路不均衡)虽非显式排队现象,但一般可用无界或单元素队列建模——如慢速错误处理 = 给一部分请求换更慢的服务时间分布;资源约束可在"出队→调度到工作线程"处建模(但堆使用/GC 这类可任意复杂)。**级联崩溃 = 多模型的直接组合**,目前最多仿真过 3 个依赖服务,规模只受工程量限制。
- **校准 (calibration)**:沿工具链从高抽象向具体推进时,用更抽象的组件**剪掉**不必要的昂贵执行(数千 CTMC 分析 → 几十个 DES → 少量 emulator → 极少压测)。前提是相邻两级行为足够接近:emulator 与压测的关键指标时间序列要一致,DES 与 emulator 同理;不一致就说明抽象漏了什么,需要人工修正(经验:多半是要"建模更多东西")。
- **最难的一环是 DES→CTMC 的反向校准**(写作时仍是 WIP):DES 状态远多于 CTMC 且转移依赖历史(非马尔可夫),映射不是近似 1-1。思路是把 DES 当 Monte Carlo 平台,跑大量随机"短走 (short walks)"直接估计转移概率,再据此调整 CTMC 的分布参数;复杂流控策略(如 admission control)可能需要在 CTMC 中显式建模。

## 相关工作中的关键区分(§8)

- **稳定 ≠ 不亚稳**:排队论研究的 stability/instability 与 metastability 本质不同——一个(排队论意义下)稳定的系统仍可能表现出亚稳态行为。
- 与 retry bug 检测类工作(SOSP'24 等)的区别:那些工作找的是代码缺陷(重试环、无超时、无限重试);本文研究的亚稳态故障**即使代码完全没有 bug**,也会纯粹由宏观反馈动力学产生。
- 网络拥塞控制(RFC 2309 谱系)和运营资源管理(如 Google STAMP)里的反馈控制思想,尚未被用于亚稳态故障——本文是切入点。

## 与 HotOS'21 框架的关系

- HotOS'21 是"是什么/为什么难"的 vision:定义 trigger / sustaining effect / hidden capacity / vulnerable 态,并列出开放问题(如何系统识别脆弱点、如何测 hidden capacity、如何非全尺寸复现)。本文直接回应其中三条:
  1. "识别脆弱点" → CTMC 状态空间可视化直接画出 vulnerable 区域边界(队列长度 × orbit 的拐点),相当于把 hidden capacity 从"隐含不可测"变成**可计算的二维分界面**;
  2. "非全尺寸复现会掩盖病因" → 用校准闭环保证各级抽象的**特征指标**(延迟、goodput、重试占比)与下一级一致,回应了 HotOS'21 "让测试环境的 characteristic metric 与全尺寸匹配"的思路;
  3. "实验法测 hidden capacity(加压→施加 trigger→观察是否自愈)" → 工具链末端输出可直接进生产压测脚本的"trigger 配置"。
- 视角差异:HotOS'21 强调 trigger 无穷、根因在 sustaining effect;本文默认 sustaining effect(重试风暴)已知,转而回答"**给定系统,参数落在什么区域会翻倒、翻倒后多久能恢复、要限流多少才能在固定时间内恢复**"这类可操作的定量问题。

## 启示

- **"先算后测"的方法论可直接搬到推理系统容量规划**:vLLM/SGLang 的调度器(队列 + batch + KV cache 容量)与本文的"队列+线程池+重试"形态同构——prefill 队列长度是 x 轴,"潜在工作放大"对应 y 轴(等待重算 KV 的请求数、被抢占后待重跑的 prefill、PD 分离下排队等 KV 传输的 decode 请求)。完全可以用 CTMC/短走 Monte Carlo 画一张"queue length × recomputation debt"的拐点图,定位调度器配置(max_num_batched_tokens、KV cache 水位、抢占策略)的 vulnerable 区域,而不是只能靠全尺寸压测碰运气。
- **稳定不等于不亚稳**对推理服务尤其重要:平均到达率 < 服务率(排队论稳定)的部署,一次 KV cache 抖动 + 重试/重新 prefill 的工作放大就可能翻倒;容量规划应以"扰动后能自愈的负载"而非稳态吞吐为安全线——与 HotOS'21 hidden capacity 一脉相承。
- **多级保真、逐级剪枝**是控制实验成本的通用范式:分析模型(秒)→ DES(分钟)→ 真实框架空业务 emulator(小时)→ 真实压测。对 vllm/sglang 这类系统,中间层可以是"真实 scheduler + 假 GPU kernel(按分布 sleep)"的 emulator——本文证明这一层能抓住 DES 抓不到的差异(如重试占比差 20%)。
- **相邻层级指标必须对账,否则抽象在说谎**:工具链的价值不在任何单一模型,而在校准闭环;差异本身是信号(emulator vs DES 的 20% 重试差)。做推理系统仿真(如 mooncake/KV cache 仿真器)时,应把"与真实集群关键指标时间序列对齐"作为仿真器验收标准。
- **可操作的输出胜过漂亮的模型**:运维真正需要的是"一个能按需翻倒系统的配置"和"限流多少能在 T 时间内恢复"。推理平台的过载控制( admission control、retry budget、降级策略)也应以这种可执行结论为交付物。
- 对 PD 分离/多级服务(mooncake 的 prefill/decode/KV 传输三段)的级联崩溃,本文"多模型直接组合"的做法(最多 3 个依赖服务)提示:逐段建模再组合,比整体黑盒压测可行得多。
